lexik_jwt_authentication:
    public_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    secret_key: '%env(resolve:JWT_PUBLIC_KEY)%'
    pass_phrase: ''
    user_id_claim: preferred_username
    clock_skew: 30
    token_extractors:
        cookie:
            enabled: true
            name: auth_token
